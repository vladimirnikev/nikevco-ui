import { SliderPointerDirective } from './slider-pointer.directive';

describe('SliderPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderPointerDirective();
    expect(directive).toBeTruthy();
  });
});
