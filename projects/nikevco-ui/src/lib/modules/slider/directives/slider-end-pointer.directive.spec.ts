import { SliderEndPointerDirective } from './slider-end-pointer.directive';

describe('SliderEndPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderEndPointerDirective();
    expect(directive).toBeTruthy();
  });
});
