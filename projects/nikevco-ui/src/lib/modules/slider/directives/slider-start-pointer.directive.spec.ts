import { SliderStartPointerDirective } from './slider-start-pointer.directive';

describe('SliderStartPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderStartPointerDirective();
    expect(directive).toBeTruthy();
  });
});
