import { SliderThumbDirective } from './slider-thumb.directive';

describe('SliderThumbDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderThumbDirective();
    expect(directive).toBeTruthy();
  });
});
